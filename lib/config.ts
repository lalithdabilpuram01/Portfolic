/**
 * Site-wide branding. Change APP_NAME here (or set NEXT_PUBLIC_APP_NAME in your
 * environment) to rename the site everywhere it appears.
 */
export const APP_NAME = process.env.NEXT_PUBLIC_APP_NAME || 'Portfolic'
